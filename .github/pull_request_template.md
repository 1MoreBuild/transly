## Summary

<!-- What changed, and why? -->

## Verification

- [ ] User-visible behavior is covered by a packaged-extension E2E journey.
- [ ] Any contract tests are limited to algorithms or protocol boundaries that E2E cannot cover reliably.
- [ ] `npm test` passes.
- [ ] `npm run package` passes.
- [ ] `git diff --check` passes.
- [ ] UI changes include current screenshots or recordings.
- [ ] No real model request ran, or the request is disclosed below.

## Release Notes

- [ ] Added a `.changes` fragment for a user-visible change.
- [ ] No fragment is needed because the change is internal, test-only, or documentation-only.

## Notes

<!-- Real-model requests, manual checks, follow-up work, or reviewer context. -->
