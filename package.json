{
  "name": "transly",
  "version": "0.1.0",
  "description": "A Chrome extension for context-aware AI translation using OpenAI-compatible APIs.",
  "private": true,
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "package": "node tools/package-extension.mjs",
    "test": "node --test src/*.test.mjs src/content/*.test.mjs src/provider/*.test.mjs"
  }
}
